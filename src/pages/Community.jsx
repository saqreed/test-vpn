import StructuredPage from '../components/StructuredPage'
import { resourcePages } from '../data/resourcePages'

export default function Community() {
  return <StructuredPage page={resourcePages.community} />
}
